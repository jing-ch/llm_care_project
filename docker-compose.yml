services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-careplan} -d ${POSTGRES_DB:-careplan}"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATABASE_URL=postgres://${POSTGRES_USER:-careplan}:${POSTGRES_PASSWORD:-careplan}@db:5432/${POSTGRES_DB:-careplan}
      - POSTGRES_HOST=db
      - POSTGRES_DB=${POSTGRES_DB:-careplan}
      - POSTGRES_USER=${POSTGRES_USER:-careplan}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-careplan}
    depends_on:
      db:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  worker:
    build: .
    command: celery -A careplan_project worker --loglevel=info
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - REDIS_URL=redis://redis:6379/0
      - POSTGRES_HOST=db
      - POSTGRES_DB=${POSTGRES_DB:-careplan}
      - POSTGRES_USER=${POSTGRES_USER:-careplan}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-careplan}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

volumes:
  postgres_data: {}
